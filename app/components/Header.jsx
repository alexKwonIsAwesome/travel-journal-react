var React = require('react');

var Header = React.createClass({
  render: function () {
   return (
     <div className="top-bar">
      <h3>Header!</h3>
     </div>
   );
  }
});

module.exports = Header;
